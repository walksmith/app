import SwiftUI
import Smith

extension Detail.Streak {
    struct Ephemeris: View {
        @Binding var session: Session
        let calendar: [Year]
        
        var body: some View {
            GeometryReader { proxy in
                VStack {
                    
                }
            }
        }
    }
}
