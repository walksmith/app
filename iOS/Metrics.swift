import CoreGraphics

struct Metrics {
    struct streak {
        struct header {
            static let width = CGFloat(100)
        }
    }
    
    struct calendar {
        struct day {
            static let size = CGFloat(45)
        }
    }
}
